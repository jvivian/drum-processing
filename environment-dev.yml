# drum-processing/environment-dev.yml
name: drumprep-dev
channels:
  - conda-forge
dependencies:
  - python=3.12
  - ffmpeg
  - numpy
  - scipy
  - pip
  # Development and testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - ruff
  - mypy
  - pre-commit
  - pip:
      - -e ".[dev]"
