# drum-processing/environment.yml
name: drumprep
channels:
  - conda-forge
dependencies:
  - python=3.12
  - ffmpeg           # conda-forge build: NVENC-enabled decode/filter/encode
  - numpy
  - scipy
  - pip
  - pip:
      - -e .
